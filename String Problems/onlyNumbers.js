//check if string contains only numbers javascript

var str = '124241';

  if(isNaN(str)){
    console.log('The string contains numbers and characters.');
  } else {
    console.log('The string only contains numbers');
  }
